# Coding Streak

Started: May 3, 2026

| Day | Date       | Chapter | Notes |
|-----|------------|---------|-------|
| 1   | 2026-05-03 | Ch 8→9  | Day 1 fresh start: doc cleanup, repo scaffold, Ch 9 diagnostic (Strong-Mid entry), system upgrades shipped, Jupyter env debugged + venv set up, first \d regex executed in notebook |
| 2   | 2026-05-04 | Ch 9    | Sprint 1 day 2 (3 of 5 sprints, stopped by choice not exhaustion). \w taught + locked (identifier-filter, mother-in-law shatter test passed). \s taught + locked at SENIOR tier (\n gotcha internalized, re.split predicted correctly). . partial — newline-as-one-character model needs Day 3 reinforcement. Locked cumulative: \d \w \s. Partial: . Remaining for Sprint 1: . [] {} * + ? |
| 3   | 2026-05-27 | Ch 9→10 | Back after a break. Finished Ch 9: alternation \|, escaping, anchors ^ $, IGNORECASE/re.I, .sub() — and built Project 3 (phone + email extractor). Took the Ch 10 core: paths + backslash trap, with open()/modes, read()/readlines(), write()/append; patdown passed, quick_notes.py append→read script working. |
| 4   | 2026-05-28 | Ch 10   | Ch 10 ALL 4 BRANCHES: pathlib deeper (Path, `/` join, .exists, .glob with `**/` recursive), os.path essentials (getcwd, join nested, basename vs dirname after a visual re-teach, abspath), shelve (dict-on-disk; writeback gotcha), pprint.pformat (save data as importable .py). Pattern: structure always right; fatigue-driven detail slips (bare unquoted names, invented shelve flags) all caught + fixed. Capstone deferred. |
| 5   | 2026-05-31 | Ch 10→11| Ch 10 CAPSTONE shipped: madlibs.py (read template → fill ADJECTIVE/NOUN/VERB → write output). Re-locked basename vs dirname cold. Slip caught: `'text'` (quoted literal) vs `text` (variable) in .write() — mirror of the usual bare-name trap. Started Ch 11 (Organizing Files): intro'd shutil.copy(src, dst); flagged os.makedirs/os.listdir as the os-module gap to fill here. |
| 6   | 2026-08-09 | Ch 11   | Back after a 10-week gap (Kenya) — double session, CH 11 COMPLETE. Core patdown re-passed (move/copy cold; delete trio 1 fix). Selective-copy project: final gate (exist_ok, endswith→bool), full line-by-line rebuild (for/if swap and FROM/TO folder confusion found + fixed), mp3 transfer variant written from memory, then ran for real — 91 PDFs collected from Downloads. New protocol: Deliver-count + hotspot scan (dots, s's, r-hugs-quote, exact names). |
| 7   | 2026-08-10 | Ch 12   | Ch 12 sections A-C COMPLETE, hands-on: ran scripts from terminal (absolute + cd/bare-name), command-vs-argument vocab, PATH mechanism + Linux ./ rule, built + activated a real venv in ~/Scripts (Git Bash: forward slashes; verify with `which python`, never trust the (.venv) label), pip install into the bubble, PATH-vs-module-search-path distinction, added Scripts folder to User Path via GUI and verified in a fresh shell. Key gate: PATH resolves the COMMAND, cwd resolves the ARGUMENT. |

Day 1 covered: full Google Doc cleanup and reconciliation, scaffolded this repo, defined the hybrid Doc+GitHub system, completed Ch 9 pre-chapter diagnostic (Strong-Mid entry), shipped praise-tier system + 3-Sentence Drill + Pomodoro 25/5/25/10 + 5-mini-projects-per-chapter rule, integrated Jupyter via VS Code, and started Sprint 1 of Ch 9 Obj 1 (taught \d, paused before drill execution).
